#include <stdio.h>
int main() {
    int length;
    scanf("%d", &length);
    int* nums = (int*)calloc(length, sizeof(int) + 1);
    int* primes = (int*)calloc(length, sizeof(int) + 1);
    for (int i = 2; i < length; i++) {

    }



    return 0;
}